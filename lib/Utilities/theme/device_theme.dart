import 'package:flutter/material.dart';

class DeviceTheme extends StatelessWidget {
  const DeviceTheme({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}