import 'package:flutter/material.dart';

class BatchDetail extends StatelessWidget {
  const BatchDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("BatchDetail"),
    );
  }
}
